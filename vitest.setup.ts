// import { vi } from "vitest";
// import { ref, computed } from "vue";

// vi.mock("vue", () => ({
//   ref: (value: any) => ({ value }),
//   computed: (fn: any) => ({
//     get value() {
//       return fn();
//     },
//   }),

//   defineComponent: (options: any) => options,
// }));
